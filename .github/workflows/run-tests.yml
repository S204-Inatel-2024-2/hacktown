name: Run API Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Create .env file
        run: |
          touch ./api/.env
          echo DATABASE_URL_TEST=${{ secrets.DATABASE_URL_TEST }} >> ./api/.env
          echo DATABASE_URL=${{ secrets.DATABASE_URL }} >> ./api/.env
          echo JWT_PUBLIC_KEY=${{ secrets.JWT_PUBLIC_KEY }} >> ./api/.env
          echo JWT_PRIVATE_KEY=${{ secrets.JWT_PRIVATE_KEY }} >> ./api/.env

      - name: Install dependencies
        working-directory: ./api
        run: npm install

      - name: Run tests
        working-directory: ./api
        run: npm run test:e2e